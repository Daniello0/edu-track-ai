/** Minimum number of quiz questions when quiz is enabled. */
export const QUIZ_QUESTIONS_MIN = 1;

/** Maximum number of quiz questions when quiz is enabled. */
export const QUIZ_QUESTIONS_MAX = 10;

/** Minimum number of answer options per quiz question. */
export const QUIZ_OPTIONS_MIN = 3;

/** Maximum number of answer options per quiz question. */
export const QUIZ_OPTIONS_MAX = 5;

/** YouTube video ID length (standard 11-character identifier). */
export const YOUTUBE_VIDEO_ID_LENGTH = 11;
